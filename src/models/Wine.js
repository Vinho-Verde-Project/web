export default class Wine {
    constructor(id, batch, productionDate, shelfLife, categoryId, taskId) {
      this.id = id;
      this.batch = batch;
      this.productionDate = productionDate;
      this.shelfLife = shelfLife;
      this.categoryId = categoryId;
      this.taskId = taskId;
    }
  }
  